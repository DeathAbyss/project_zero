{
  "_comment_": "Configuração mínima do Claude Code pra ativar os hooks do template. Renomeie pra .claude/settings.json no projeto destino. Se já existir settings.json, mescle os blocos manualmente.",

  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/check-sync-project-map.sh"
          }
        ]
      }
    ]
  }
}
